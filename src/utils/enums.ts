export enum Status {
  dead = "dead",
  alive = "alive",
  unknown = "unknown",
  all = "all"
}

export enum Gender {
  male = "Male",
  female = "Female",
  other = "Other"
}

export enum ModalState {
  add = "add",
  edit = "edit",
  delete = "delete",
}